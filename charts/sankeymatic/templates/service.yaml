apiVersion: v1
kind: Service
metadata:
  name: {{ include "sankeymatic.fullname" . }}
  labels:
    {{- include "sankeymatic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sankeymatic.selectorLabels" . | nindent 4 }}
