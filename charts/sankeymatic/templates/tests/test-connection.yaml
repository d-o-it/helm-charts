apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sankeymatic.fullname" . }}-test-connection"
  labels:
    {{- include "sankeymatic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sankeymatic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
