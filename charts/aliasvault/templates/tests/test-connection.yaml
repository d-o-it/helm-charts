apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aliasvault.fullname" . }}-test-connection"
  labels:
    {{- include "aliasvault.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aliasvault.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
