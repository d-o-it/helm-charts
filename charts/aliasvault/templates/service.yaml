apiVersion: v1
kind: Service
metadata:
  name: {{ include "aliasvault.fullname" . }}
  labels:
    {{- include "aliasvault.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aliasvault.selectorLabels" . | nindent 4 }}
